: add build targets in makefile under various options for fun

: build migration script that runs all migration SQL files

: edit pg deployment to also insert data from a dump

: chron a thing that does a regular pg_dump and pushes somewhere

: build deployment scripts for go and nginx

: build master deployment script

: ip_data project for ver0.2.0
    
: use stored procedure in postgres to ensure safety on api call inserts (e.g. "button" for "socialvisits" can only be "twitter," "github," or "linkedin"

: add some api security? 

remember to make run go server after changes





javascript needs to add onclick handler to socials icons, which sends which button it is to the api (also page)
make sure that this actually works - if you navigate away does it actually call it?
	may need to make the icons not links?
	or track on hover instead of on click?
